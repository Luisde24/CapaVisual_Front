import { DataTimeFormatPipe } from './data-time-format.pipe';

describe('DataTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DataTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
